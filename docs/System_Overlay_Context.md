# SYSTEM OVERLAY CONTEXT
**WARNING TO AI AGENTS: READ THIS BEFORE EXECUTING TASKS**

## 1. Core System Knowledge
Before taking any action, you must understand that this environment is a tiny node within a massive, strictly architected Life Operating System (LOS / PMTOS). 

For a complete understanding of the system's infrastructure, rules, and operations, you must immediately locate and read the following core files:
1.  `TS - System Architecture Map.md`
2.  `LOS_Taxonomy.json` (or `PMTOS_Taxonomy.json` if in the Playmetech branch)

## 2. Your Current Environment (The Overlay)
You are currently operating inside a bounded "Overlay." An overlay is a transversal project or workspace that spans across the system without breaking the physical folder taxonomy. Examples of Overlays include NotebookLM notebooks, Google Workspace Projects, or Antigravity Agent environments.

Because you are operating in an Overlay, you are strictly bound by the following systemic rules.

## 3. The Strict Nomenclature Rule
**NEVER invent arbitrary tags, folders, labels, or categories.**
This environment is mathematically audited. All "buckets" or transversal projects MUST strictly align with the predefined taxonomy JSONs. If you are asked to categorize, label, or create a new project, you must pull from the exact Taxonomy list.

*   **Base Buckets:** Broad, high-level captures must map perfectly to an L2 taxonomy node (e.g., `01 01 00 Personal Admin`).
*   **Specific Overlays:** Highly focused work must perfectly match a specific project name (e.g., `01 05 01 Projects > 20260509 LFC-CFC`).

## 4. The Duality Rule (Cloud vs. Local Disk)
If you are generating scripts, maintaining paths, or interacting with the physical disk (Mac Mini), you must respect the fundamental boundary between Cloud Storage and Local Execution:

*   **The Cloud (Google Drive):** Follows strict `Title Case` naming with numeric prefixes (e.g., `01 Playmetech Admin / 01 The System (TS)`).
*   **The Local Disk (Mac Mini):** Follows strict `snake_case` programming rules and strips the numeric prefixes for clean execution (e.g., `/Users/daniel/Documents/AGY/agy_pmt/the_system_pmt/`).

These physical paths are enforced by python auditors. Never create stray folders. Never write code that expects the Local Disk to use the Cloud naming conventions.

If you encounter ambiguity, stop and ask the User. Do not guess. Do not create technical debt.
