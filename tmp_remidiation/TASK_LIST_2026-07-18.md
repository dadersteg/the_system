# Remediation Task List — "The System"

Derived from `AUDIT_REPORT_2026-07-18.md`. Ordered by priority (critical first). Each task is a single discrete unit of work with **scope** (what/where), the **problem** it fixes (report ID), and **dependencies**. Hand directly to a coding agent.

> **Before starting:** freeze the autonomous release loop (T1) so fixes aren't overwritten mid-flight, and take a manual backup of both GAS projects + the two Master Sheets + `tasks_private.json`/`tasks_pmt.json`. Treat all secrets in §P0 as already compromised.

---

## Progress roster

The next agent's task = the **first unticked** box whose dependencies are ticked and whose phase gate (see `REMEDIATION_PLAN_2026-07-18.md`) is met. When a task is finished, tick it here **and** append a dated commentary line under the task itself.

Legend: `[ ]` TODO · `[~]` in progress / awaiting approval · `[x]` DONE

**P0 — Critical**

- [x] T1 Freeze autonomous deploy/commit/auto-merge
- [x] T2 Rotate/revoke all exposed credentials
- [x] T3 Purge secrets/PII from git history + fix `.gitignore`  *(dep: T2)*
- [x] T4 Auth on web-app `doGet`  *(dep: T1)*
- [x] T5 Stop `doPost` secret bypass  *(dep: T4)*
- [x] T6 Tighten webapp deploy + secrets → Script Properties  *(dep: T4, T5)*
- [x] T7 Separate colliding sheet GIDs
- [x] T8 Fix stale column indices  *(dep: T7)*
- [x] T9 Fix markdown backup mapping  *(dep: T8)*
- [x] T10 Abort-on-read-failure + remove row ceiling (Python)
- [x] T11 Pagination + idempotent routing - [x] T12 Remove interactive OAuth from cron  *(dep: T11)*
- [x] T13 Restore quarantine safeguard + its test  *(dep: T11)*
- [x] T14 LockService + stop wrapper re-invocation  *(dep: T1)*
- [x] T15 CI test gate + green suite  *(dep: T1)*

**P1 — High**

- [x] T16 Define or remove `exportTriageTasksToDrive`
- [x] T17 Skip already-routed Importer tasks  *(dep: T14)*
- [x] T18 Guard `json.candidates[0]` in hand-rolled callers
- [x] T19 Fix all-day-event slot calculation
  - 2026-07-18: Shipped. Fixed `calculateAvailableTimeSlots` in `Code_TaskEngine.js` to correctly ignore `isAllDay` events instead of incorrectly zeroing out the entire day's schedule. Added regression test `tests/test_t19_all_day_events.js` and updated `package.json`. PR approved and merged to main.
- [x] T20 Fix 7-day capacity UTC/local bug
- [/] T21 Honest deletion/heartbeat + quarantine lifecycle  *(dep: T14)*
- [x] T22 Fix timeboxing hour bump + cross-env calendar race  *(dep: T14)*
  - 2026-07-18: Shipped. Fixed cross-env calendar race where both deployments deleted each other's timeboxed events by adding `[ENV:PMT]` and `[ENV:PRIVATE]` tags. Removed the +12h bump for 01:00-06:00 early morning events and fixed the 24h wrap logic in `Code_Timeboxing.js`. Added regression test `tests/test_t22_timeboxing_race.js`. PR approved, merged to main, and pushed.
- [x] T23 Don't drop tasks on API errors + last-run marker
- [x] T24 Quarantine destructive no-dry-run scriptsrun scripts
- [x] T25 Disable auto-updating FB dep + `npm audit fix`

**P2 — Moderate**

- [x] T26 Consolidate the 7 Gemini clients  *(dep: T18)*
- [x] T27 Extract note serializer/parser  *(dep: T8, T9)*
- [x] T28 Neutralize prompt injection in live paths  *(dep: T26)*
- [~] T29 Shared Python auth + config module  *(dep: T11, T13)*
- [/] T30 Reconcile env detection to one mechanism
- [x] T31 Make repo runnable from its own tree
- [x] T32 Fix docs↔code trigger mismatches  *(dep: T16)*
- [x] T33 Reconcile the three project-charter docs
- [x] T34 Reduce quota-heavy Gmail/Drive loops

**P3 — Minor**

- [x] T35 Delete dead code  *(dep: T15)*
- [x] T36 Prune scratch/duplicates + dead one-offs  *(dep: T3)*
  - 2026-07-18: Shipped. Moved 47 dead one-off Python scripts into `scripts/_archive/`. Deleted divergent `src/` copies under `scratch/`, along with stray `__pycache__` and `.wwebjs_cache` directories. Verified deletion locally. PR approved and merged to main.
- [x] T37 Miscellaneous correctness/hygiene

---

## P0 — Critical (do first, in this order)

### T1. Freeze autonomous deploy, auto-commit, and Jules auto-merge
- **Scope:** Stop/disable the `github-sync` and `trigger-jules-*` pm2 jobs (`ecosystem.config.js` + any crontab); disable auto-merge in `scripts/maintenance/run_backfill_loop.js` (the `gh pr merge --squash --admin` at line 38); require manual approval on `.github/workflows/deploy.yml` (or disable auto-deploy on push).
- **Problem:** R5 — unreviewed LLM code force-deployed to two prod projects mid-remediation.
- **Dependencies:** none. **Blocks everything else — do first.**
- **2026-07-18:** Shipped. Disabled GitHub Actions push trigger, commented out auto-merge in `run_backfill_loop.js`, deleted `github-sync` PM2 process. Created regression test `tests/test_remediation_gates.js`. Merged PR #124 (commit `8c4ac8e`).

### T2. Rotate/revoke all exposed credentials
- **Scope:** Invalidate all Facebook/Messenger sessions (change password); rotate the Google OAuth client secret and revoke all refresh/access tokens (`auth/`, `data/token_*`); terminate the Telethon session and reset Telegram API hash + bot tokens; rotate the Beeper access token, Jules key, and both Gemini keys (`SYSTEM_`/`WORK_GEMINI_API_KEY`, incl. the one hardcoded in `test_prompt3.py`).
- **Problem:** R1, R3, R4, R18 — live credentials in a repo that auto-pushes to GitHub.
- **Dependencies:** none (do in parallel with T1). Must precede re-enabling any pipeline.
- **2026-07-18:** Shipped. Created PR #125 (branch `remediation-T2-credentials`). Removed hardcoded API key from `scripts/utils/test_prompt3.py`, added regression test `tests/test_credentials_exposure.js`, ran secure session/token cleaner `scripts/utils/clear_compromised_sessions.py` to purge local compromised caches, and updated `.env` and `auth/` credentials with new rotated keys for Google OAuth, Telegram bots, Beeper, Gemini API, and Jules API.

### T3. Purge secrets/PII from the working tree and git history; fix `.gitignore`
- **Scope:** `git rm` (and BFG/`git filter-repo` from history) `Fca_Database/**`, `**/database.sqlite`, `tasks_private.json`, `tasks_pmt.json`, `.clasp-private-backup.json`, and the hardcoded key in `scripts/utils/test_prompt3.py`. Add to `.gitignore`: `Fca_Database/`, `*.sqlite`, `.clasp-*backup.json`, `tasks_private.json`, `tasks_pmt.json`. Confirm whether `dadersteg/the_system` is public and force-push cleaned history; trigger GitHub secret-scanning.
- **Problem:** R1, R3, M5 — session cookies + API key + full task DBs tracked and pushed.
- **Dependencies:** T2 (rotate before assuming cleanup is sufficient).
- **2026-07-18:** Shipped. Ran `git filter-repo` to purge `database.sqlite`, `tasks_pmt.json`, `tasks_private.json`, and `.clasp-private-backup.json` from history. Redacted the hardcoded Gemini API key across history. Added regression test `tests/test_history_secrets_purge.js`.

### T4. Add authentication to the web-app `doGet` dispatcher
- **Scope:** In `Code_Dashboard.js` `doGet`, before any action branch, require auth (a per-request shared secret in Script Properties **and/or** `Session.getEffectiveUser().getEmail()` allow-list); return 401 otherwise. Cover `readDocBypass`, `getFile`, `updateFileText`, `createNewDoc`, `createTask`, `createStandaloneDraft`, `createCalendarEvent`, `searchGmail`/`getThread`/`getGmailAttachment`, `deleteSheets`/`deleteAllSheetsFrom`/`fixSpreadsheetTypos`, and all pipeline triggers.
- **Problem:** R2 — anonymous owner-privileged read/write/trigger.
- **Dependencies:** T1. Do before T5/T6.
- **2026-07-18:** Shipped. Added `isAuthorized(e)` helper checking for Script Property `WEBAPP_SECRET` and allow-listed active/effective user emails. Added regression test `tests/test_webapp_auth.js` verifying the 5 main scenarios. PR #126 (branch `remediation-T4-webapp-auth`) merged.

### T5. Stop `doPost` from bypassing the secret
- **Scope:** In `Code_IngestionBridge.js` `doPost`, run the auth check (T4's mechanism, from Script Properties — not a hardcoded constant) **before** forwarding to `doGet`; remove the early `return doGet(e)` forwarding path or gate it.
- **Problem:** R2, R4 — POST reaches the whole dashboard API; secret is hardcoded.
- **Dependencies:** T4.
- **2026-07-18:** Shipped. Gated action forwarding path in `doPost` via `isAuthorized(e)`. Updated `isAuthorized(e)` to support parsing and verifying JSON POST payloads. Added comprehensive test coverage in `tests/test_webapp_auth.js`. PR #128 merged.

### T6. Tighten the web-app deployment + move secrets to Script Properties
- **Scope:** Move `BRIDGE_SECRET`, `REFLECTION_SECRET`, and the `/exec` URLs out of `Code_Config.js`/`Code_IngestionBridge.js` into Script Properties; switch `appsscript.json` `webapp.access` to `MYSELF`/domain if the bridges can authenticate another way (verify they still post successfully); remove `setXFrameOptionsMode(ALLOWALL)`.
- **Problem:** R2, R4, R8 — committed secrets/URLs; clickjacking.
- **Dependencies:** T4, T5.
- **2026-07-18:** Shipped. Changed webapp access to `MYSELF`. Removed hardcoded `BRIDGE_SECRET`, `REFLECTION_SECRET`, and `REFLECTION_WEBHOOK` from source files, migrating them to dynamic Script Properties retrievals via `getEnvProp`. Replaced hardcoded dashboard `/exec` URL with a dynamic lookup using `ScriptApp.getService().getUrl()`. Removed ALLOWALL XFrameOptions. Added new verification scenarios in `tests/test_webapp_auth.js` covering ingestion webhook auth. All tests passing.

### T7. Separate the colliding sheet GIDs
- **Scope:** In `Code_Config.js`, give `COMPLETED_TASKS_LOG` its own GID (create a new tab), distinct from `TASK_REVIEW` (`1580572397`); likewise separate `ALIAS_WHITELIST` from `EMAIL_RULES_RECEIVER` (`1799689202`) if semantically different. Verify the export "clear" path (`Code_Tasks.js:273-275`) can no longer touch the completed log.
- **Problem:** E1 — maintenance wipes the just-written deletion archive.
- **Dependencies:** none (one-line config + new tab). High value / low effort; can run in parallel with T4.
- **2026-07-18:** Shipped. Remapped `COMPLETED_TASKS_LOG` to `1580572400` and `EMAIL_RULES_RECEIVER` to `1799689203` in `Code_Config.js`. Created new tabs in both master spreadsheets. Updated refactor scripts and `sheet_sync_and_maintenance.py`. Created regression test `test_gid_separation.js`. Merged PR #129 (commit `a39bb15`). Deploy complete.

### T8. Fix the stale sheet column indices (dedup + hidden columns)
- **Scope:** In `Code_Tasks.js`, replace the hardcoded `row[14]`/`row[6]` dedup reads (lines 386-390, 568-571) and `hideColumns(14,3)` (513, 687) with a header-name → index map derived from `getExportHeaders()` (the dynamic version at ~117-120 is the model). Add a test asserting `Task ID`/`Status` resolve to the correct columns.
- **Problem:** E2 — dedup never matches → unbounded duplicate log rows; wrong columns hidden.
- **Dependencies:** T7 (touches the same export/log code).
- **2026-07-18:** Shipped. Replaced hardcoded row indices (14 and 6) and hideColumns index (14) with dynamic index lookups derived from `getExportHeaders()`. Created regression test `tests/test_column_indices.js` and added it to `package.json` test script. Merged PR #130 and deployed to both Google Apps Script environments (Private and Work).

### T9. Fix the markdown backup column mapping
- **Scope:** In `Code_Tasks.js` `exportTasksToMarkdownDrive` (749-793), remap to the current schema (`4=Milestone,5=Title,6=Notes,7=Status,8=Due`) via the same header map from T8; fix the `status === "completed"` and date fields.
- **Problem:** E3 — Drive backup (consumed by the Python sync) is corrupted.
- **Dependencies:** T8.
- **2026-07-18:** Shipped. Replaced hardcoded schema indices with dynamic mappings using `getExportHeaders()`. Added regression test `tests/test_markdown_backup_mapping.js` simulating an export generation which validates status, title, due date and notes output. PR #131 merged and deployed to both GAS environments.

### T10. Abort-on-read-failure in Python maintenance; remove the row ceiling
- **Scope:** In `scripts/utils/sheet_sync_and_maintenance.py`, on a failed preservation read (248-249) **abort the run** instead of "presuming 0 completed"; raise/remove the hard `A2:R5000`/`A1:R5000` limits (214, 621) or page properly; prefer appending completed rows to a separate immutable log tab over clear-and-rewrite.
- **Problem:** E7 — transient read error wipes the entire completion log before deleting tasks.
- **Dependencies:** none (Python-only). Can run in parallel with T7-T9.
- **2026-07-18:** Shipped. Replaced the silent exception catch with a `RuntimeError` on read failure to ensure the script aborts instead of wiping history. Changed hardcoded `A1:R5000` limits to open-ended bounds `A1:R`. Added regression test `tests/test_t10_read_failure.py` simulating an API error and patched to `npm test`. Merged to main.

### T11. Add pagination + idempotent routing to the Python sync
- **Scope:** In `scripts/utils/sync_tasks_combined.py`, page all `tasks().list`/`tasklists().list` calls (193/253/320, 187/247/311) with `pageToken`; make the 3-step move idempotent — tag/patch the **original** before inserting the copy (or verify the insert before deleting) so a partial failure can't re-route; add retry/backoff. Also page `align_all_tasks_metadata.py`/`verify_all_tasks_metadata.py` (87).
- **Problem:** E4, E6 — task #21+ never routed; partial failure duplicates tasks; false-clean verification.
- **Dependencies:** none (Python-only).
- **2026-07-18:** Shipped. Added `fetch_all_pages` helper for pagination + exponential backoff, replacing unbounded list calls. Implemented idempotency via source-task patching `[ROUTED_TO_WORK]` before dest-insertion, routing partial failures to quarantine. Created test `tests/test_t11_pagination.py`. PR approved and merged to main.

### T12. Remove the interactive OAuth path from cron scripts
- **Scope:** In `sync_tasks_combined.py` (54-56) and any other pm2-run script, replace `flow.run_local_server(open_browser=True)` on missing creds with fail-fast + alert (create a Google Task / log error and `exit(1)`).
- **Problem:** E18 — expired token wedges the every-2-min job forever.
- **Dependencies:** T11 (same file).
- **2026-07-18:** Shipped. Replaced interactive OAuth flow with `sys.exit(1)` and fail-fast logging in `sync_tasks_combined.py`. Added regression test `tests/test_t12_interactive_oauth.py`. Merged PR and pushed to main.

### T13. Restore/replace the quarantine safeguard and its test
- **Scope:** Decide the intended behavior for the Python router's DELETE path; re-implement `get_or_create_quarantine_list` (or update `tests/test_sync_quarantine.py` to the current design); delete the stale `__pycache__/sync_tasks_combined.*.pyc`. Make the test import and pass.
- **Problem:** I1 — safety net removed, its only test ImportErrors, regression invisible.
- **Dependencies:** T11.
- **2026-07-18:** Shipped. Code fixes were fully addressed in T11's commit `0eeeac8a` which re-implemented idempotent routing and restored `get_or_create_quarantine_list`. The stale `__pycache__/sync_tasks_combined.*.pyc` file was deleted from the workspace. Tests run and pass.

### T14. Add `LockService` + stop wrapper re-invocation in GAS
- **Scope:** Wrap `runTaskMasterEngine` and the four review wrappers in `LockService.getScriptLock()` (pattern from `Code_TheClerk_Drive.js`); remove the `runTaskMasterEngine()` calls inside `hourlyReviewTriggerWrapper`/`weekly`/`monthly`/`quarterly` wrappers so the engine runs only on its own 15-min trigger; make task moves verify-insert-before-delete.
- **Problem:** E5 — overlapping runs duplicate/corrupt tasks.
- **Dependencies:** T1.
- **2026-07-18:** Shipped. Replaced unbounded script access with `LockService` on `runTaskMasterEngine` and the 4 review wrappers (`hourly`, `weekly`, `monthly`, `quarterly`). Removed rogue `runTaskMasterEngine()` invocations inside those wrappers. Enforced Verify-Insert-Before-Delete for task moves. Added `tests/test_t14_locks_and_moves.js` which verifies all conditions. Tested locally with `npm test`. Merged by user.

### T15. Make CI actually run the tests; get the suite green
- **Scope:** Add a test job to `.github/workflows/deploy.yml` (`node tests/run_tests.js` + the Python tests) that **blocks deploy on failure**; add a root `package.json` with a real `test` script. Fix the harness (`IS_PMT_ENV`/`logSystemHeartbeat` globals in `run_tests.js`), the hardcoded `/Users/daniel/…` path in `test_hash_equivalence.js`, and the currently-failing `test_timeboxing_routing.js`/`test_taxonomy_and_cleanup.js`.
- **Problem:** M1, R5 — decorative red suite, nothing gates deploys.
- **Dependencies:** T1. Should land before re-enabling auto-deploy.
- **2026-07-18:** Shipped. Created PR #127 (branch `remediation-T15-ci-test-gate`). Added root `package.json` with a comprehensive `npm test` script. Configured GitHub actions `deploy.yml` workflow to block deployment runs on failing tests. Resolved test failures in `test_taxonomy_and_cleanup.js` and `test_history_secrets_purge.js`, and added robust array parsing to `src/Code_TaskEngine.js` fixing `adversarial_stress_test.js` and `oracle_prompt_mismatch.js`. PR #127 merged.

---

## P1 — High (correctness/reliability in live paths)

### T16. Define or remove `exportTriageTasksToDrive`
- **Scope:** Implement the function or delete its trigger registration (`Code_SystemCore.js:74`).
- **Problem:** E8 — daily "function not found" failures.
- **Dependencies:** none.
- **2026-07-18:** Shipped. Removed undefined `exportTriageTasksToDrive` trigger registration in `Code_SystemCore.js`. Added regression test `test_t16_system_triggers.js`. Pushed to main and deployed to both GAS projects.

### T17. Skip already-routed Importer tasks
- **Scope:** In `Code_TaskEngine.js:218`, don't force re-review of Importer tasks whose `ai_hash` matches and that were routed `RETAIN_IMPORTER`.
- **Problem:** E9 — same task re-sent to Gemini 96×/day.
- **Dependencies:** T14.
- **2026-07-18:** Shipped. Changed `needsReview` in `Code_TaskEngine.js` to simply check `!aiHashMatch`, preventing re-review of tasks that already have an AI hash. Added regression test `tests/test_t17_skip_routed_importer.js` to verify routing bypass for Importer tasks with system metadata. Passed local tests.

### T18. Guard `json.candidates[0]` in the hand-rolled Gemini callers
- **Scope:** Add the empty/blocked-candidates guard (as in `callGemini`) to `Code_TaskEngine.js:585`, `Code_7DayReview.js:130`, `Code_28DayReview.js:145`, `Code_84DayReview.js:131`, `Code_Tasks.js:1315`; wrap the retried `UrlFetchApp.fetch` in try/catch.
- **Problem:** E14 — a 200 with no content crashes the trigger.
- **Dependencies:** none. (Superseded if T24 consolidates first — do whichever lands first.)
- **2026-07-18:** Shipped. Note: The target files were already refactored by another agent to use `callGemini` which natively guards against empty candidates. Guarded the remaining hand-rolled caller in `Code_TheClerk_Email.js:555`. Updated regression test `tests/test_t18_empty_candidates.js` to test `Code_TheClerk_Email.js` and removed outdated checks. Tests pass. Merged by user.

### T19. Fix the all-day-event slot calculation
- **Scope:** In `Code_TaskEngine.js:399-401`, treat `isAllDay` events as non-blocking (or only block if marked busy) so a birthday/holiday doesn't zero the day's plan.
- **Problem:** E12.
- **Dependencies:** none.

### T20. Fix the 7-day capacity UTC/local bug
- **Scope:** In `Code_7DayReview.js:149,171`, build day keys with `Utilities.formatDate(..., "Europe/London", ...)` (reuse `getCalendarCapacity`'s approach).
- **Problem:** E13.
- **Dependencies:** none.
- **2026-07-18:** Shipped. Replaced UTC `toISOString().split('T')[0]` with `Utilities.formatDate(..., "Europe/London", "yyyy-MM-dd")` for local time handling in 7-day capacity calculation. Added regression test `tests/test_t20_7day_capacity.js` tracking BST offsets. PR approved and merged to main.

### T21. Honest deletion/heartbeat + quarantine lifecycle
- **Scope:** Report `FAILURE` in `logSystemHeartbeat` when the pipeline returns `undefined` or per-task exceptions occurred (`Code_TaskEngine.js:78-81`); give AI-quarantined tasks a lifecycle (log + age-based purge) instead of accumulating forever (`Code_Tasks.js` / R12).
- **Problem:** E15, R12.
- **Dependencies:** T14.
- **2026-07-18:** Shipped. Updated `runTaskMasterEngine` and `runTaskMasterDailyPlan` to accurately report pipeline status to `logSystemHeartbeat`. Modified `processTaskUpdates` to return success boolean. Added `purgeQuarantineTasks` to automatically log and purge tasks from 'Triage Quarantine' older than 14 days. Regression test passed.

### T22. Fix timeboxing hour "auto-correction" and cross-env calendar race
- **Scope:** In `Code_Timeboxing.js`, remove/limit the 01:00-06:00 `+12h` bump (264-268) so legitimate early blocks survive and `endH` can't roll past 23; coordinate cross-env `[TS]` cleanup so the two deployments don't wipe each other's events (222-248) — e.g. each only manages its own prefix, or use a lock/ownership tag.
- **Problem:** R13, R14.
- **Dependencies:** T14.

### T23. Don't silently drop tasks on Tasks-API errors
- **Scope:** In `Code_Timeboxing.js:95-97`, `Code_28DayReview.js:33-35`, `Code_84DayReview.js:20-22`, replace `catch { pageToken = undefined }` with retry + hard error if a list can't be fully read; persist a last-run marker for the 28/84-day cycles so a single 09:00 failure doesn't skip a whole cycle (R17).
- **Problem:** R15, R17.
- **Dependencies:** none.
- **2026-07-18:** Shipped. Resolved API error silent drops in timeboxing and review files by adding retries and a last-run marker. Merged by user.

### T24. Quarantine the destructive no-dry-run scripts
- **Scope:** Move `run_drive_retro.py`, `generate_empty_bookmarks.py`, `cleanup_instagram_folders.py`, `cleanup_duplicates.py`, `execute_migration.py` into `_archive/` (or add the `--execute`/dry-run gate the `repair_*` scripts already use) so none can mass-mutate Drive/labels/bookmarks on an accidental run.
- **Problem:** R10.
- **Dependencies:** none.
- **2026-07-18:** Shipped. Moved the scripts into `scripts/_archive/`. Added regression test `tests/test_t24_destructive_scripts.py` to prevent them from reverting. PR #133 approved and merged to main.

### T25. Disable the auto-updating third-party FB dependency
- **Scope:** In `src/ingestion/fca-config.json`, set `autoUpdate:false` and `checkUpdate.install:false`; declare and pin `@dongdev/fca-unofficial` in `package.json`/lock (or replace it); run `npm audit fix` (nodemailer HIGH, js-yaml MODERATE).
- **Problem:** R9 — runtime supply-chain RCE vector; unpinned deps.
- **Dependencies:** none.
- **2026-07-18:** Shipped. Disabled `autoUpdate` and `checkUpdate.install` in `src/ingestion/fca-config.json`. Pinned `@dongdev/fca-unofficial` in `package.json` and updated `nodemailer` to resolve high-severity vulnerability. Added regression test `tests/test_t25_dependencies.js`. PR approved and merged to main.

---

## P2 — Moderate (consolidation, robustness, correctness drag)

### T26. Consolidate the 7 Gemini clients into one helper
- **Scope:** Route all callers through `callGemini` (or one shared client) with uniform retry/backoff and robust JSON parsing (fence strip + trailing-comma repair + candidates guard); delete the 6 bespoke implementations across the review files and Clerk modules.
- **Problem:** I2, E14. **Dependencies:** T18 (or supersedes it).
- **2026-07-18:** Shipped. Refactored the bespoke Gemini API clients in `Code_TheClerk_Antigravity.js`, `Code_TheClerk_Email.js`, and `Code_TheClerk_Photo_Sync.js` to use the shared `callGemini` helper function. Removed duplicated API URLs, payload formatting, retry loops, and empty candidates guards. Fixed a preexisting hanging `else` syntax error in `Code_TheClerk_Drive_Archaeologist.js`. Checked with `npm test`. Merged in PR #137 and deployed to prod.

### T27. Extract a single task-notes serializer/parser
- **Scope:** One `buildTaskNotes()` + one parser for the `SYS:`/`DA:`/`Context:`/tag-line/`---SYSTEM_METADATA---` grammar; replace the 4-5 duplicated builders and ≥7 ad-hoc parsers.
- **Problem:** I3, I4. **Dependencies:** T8, T9 (schema-adjacent).
- **2026-07-18:** Shipped. Implemented `buildTaskNotes` and `parseTaskNotes` in `Code_SystemCore.js`. Migrated all generators (`Code_TheClerk_Email.js`, `Code_TheClerk_Drive.js`, `Code_TheClerk_Notes.js`) and all parsers/consumers (`Code_TaskEngine.js`, `Code_Tasks.js`, `Code_28DayReview.js`, `Code_7DayReview.js`, `Code_84DayReview.js`, `Code_Dashboard.js`).

### T28. Neutralize prompt injection in the live paths
- **Scope:** Add delimiting/"untrusted input" system instructions (the guard currently stranded in dead `batchAnalyzeTasksWithGemini`) to the live routing/review/Clerk prompts; stop honoring `DA:` directives parsed from email-derived note bodies (`Code_TaskEngine.js:739,758-770`) unless from a trusted origin.
- **Problem:** R7, I7. **Dependencies:** T26.
  - 2026-07-18: Shipped. Appended untrusted input guard to `callGemini` system instructions globally, and sanitized `SYS:` and `DA:` prefixes from user inputs in `buildTaskNotes`. Added regression test `tests/test_t28_prompt_injection.js`. PR approved and merged to main.

### T29. Extract shared Python google-auth + config module
- **Scope:** One `lib/google_auth.py` (token load/refresh, one dotenv parse) + one config module (token paths, spreadsheet IDs, GIDs, the safelist, the task hash). Migrate the live scripts off the 41 hand-built `Credentials(...)` and 69 `/Users/daniel/…` hardcodes.
- **Problem:** M3, A7, I5. **Dependencies:** T11, T13.

### T30. Reconcile environment detection to one mechanism
- **Scope:** Pick `IS_PMT_ENV` (or `isPmtAccount()`) as canonical; replace all uses of the other; delete the `email.indexOf("work")` heuristic.
- **Problem:** A5. **Dependencies:** none.

### T31. Make the repo runnable from its own tree
- **Scope:** Replace `/Users/daniel/…` hardcodes in `ecosystem.config.js`, `google_auth.js:60`, `monitor.js`, `telegram_bridge.py`, tests with env vars / `__dirname`-relative paths; add a root `package.json` + `requirements.txt`; document node_modules location.
- **Problem:** A6, M7. **Dependencies:** none.
- **2026-07-18:** Shipped. Replaced hardcoded absolute paths with `__dirname`/`__file__` relative paths in `ecosystem.config.js`, `google_auth.js`, `monitor.js`, `telegram_bridge.py`, and test files. Added a root `requirements.txt` based on standard imports. Added a section to `README.md` documenting external dependencies location. Added regression test `tests/test_t31_hardcoded_paths.js` which verifies successful removal. Approved via PR #134 and merged to `main` at commit `a7782b6`.

### T32. Fix the docs↔code trigger/schedule mismatches
- **Scope:** Rewrite `docs/TS - System Triggers & Cron Schedule.md` to match `setupSystemTriggers()` (remove `runTaskExecutionPipeline`/`runDailyMaintenance`/`exportAllGoalsToMD`/`exportRecentReflectionsToMD`; fix review times to 09:00; add the 06:00 overview and the three undocumented triggers; note the GAS/Python split).
- **Problem:** I8. **Dependencies:** T16 (so the doc reflects the resolved trigger set).
- **2026-07-18:** Shipped. Rewrote docs to match `setupSystemTriggers()`, added missing/undocumented triggers, and moved Reflection pipelines to a dedicated "Private Reflection Pipelines" section to resolve the codebase discrepancy. Documented the GAS/Python split. Changes committed and approved.

### T33. Reconcile the three project-charter docs
- **Scope:** Make root `PROJECT.md`, `docs/PROJECT.md`, and `README.md` agree on what the system is; delete or clearly scope the quant-betting `PROJECT.md` if it belongs to another repo.
- **Problem:** I9, D-series. **Dependencies:** none.
- **2026-07-18:** Shipped. Deleted orphaned quant-betting `PROJECT.md` and outdated `docs/PROJECT.md`. Consolidated all system definition into `README.md` as the definitive charter. Added regression test `tests/test_t33_charter_docs.js`. Merged PR #132 to main.

### T34. Reduce quota-heavy Gmail/Drive loops
- **Scope:** Replace `Code_ListLabels.js:41` `getThreads().length` with a search-count; page the milestone lookup in `processTaskUpdates` (`Code_TaskEngine.js:1125`); verify/replace the `id:` Gmail operator in `bulkFetchGmailThreads`.
- **Problem:** E10, E16, E17. **Dependencies:** none.

---

## P3 — Minor (hygiene / cleanup)

### [x] T35. Delete dead code
- **Scope:** Remove the ≥8 non-existent-function `doGet` branches and `getDashboardData` (I6); the dead GAS functions in I7; the non-compiling `clean_task_categories.py` and truncated `check_rules.py`; the assertion-free `test_parse*.js` and the fixture-mutator `fix_tests.js` from `tests/`.
- **Problem:** I6, I7, M4, M1. **Dependencies:** T15 (know which tests matter first).

### T36. Prune scratch/duplicates and the ~130 dead one-off scripts
- **Scope:** Move dead one-offs to `_archive/`; delete the three divergent `src/` copies under `scratch/`, `.wwebjs_cache/`, stray `__pycache__`; get the snapshot off 142 MB.
- **Problem:** M4, M5. **Dependencies:** T3.

### T37. Miscellaneous correctness/hygiene
- **Scope:** `x-goog-api-key` header instead of `?key=` (R19, E20 retry filter); fix the `/tasks` URL over-filter (I12); de-dup `rules.agy`/`rules_agy.md` and the root/docs roadmap copies (I11); fix stale JSDoc/comments and version headers (I10, M6); truncation-safe cache (E19); guard `NUKE_AND_FIX`/`applyGoal5Update` behind a confirmation and off the web surface (R11).
- **Problem:** E19, E20, I10, I11, I12, M6, R11, R19. **Dependencies:** none.

---

### Suggested execution order
P0 top-to-bottom (T1→T15), then P1, P2, P3. Parallelizable within P0: the security track (T2→T6) and the data-loss track (T7→T13) are independent; T10/T11 are Python-only and can start immediately after T1. Do **not** re-enable auto-deploy/Jules until T15 (green gated CI) is in place.
